rule create_variant_exclude:
    shell:
        """
        bcftools query -f '%ID\\n' -i \
                'INFO/AVG_CS < {params.avg_call} || \
                INFO/R2 < {params.rsq_common} || \
                (INFO/MAF < {params.maf_thresh} && INFO/R2 < {params.rsq_rare})' \
            {input.info} > \
            {output}
        """

rule create_variant_exclude_delim:
    shell:
        """
        gunzip -c {input} | perl -lane 'print $F[0] if \
                $F[5] < {params.avg_call} | \
                $F[6] < {params.rsq_common} | \
                ($F[4] < {params.maf_thresh} & $F[6] < {params.rsq_rare})' > \
            {output}
        """

rule filter_vcf:
    params:
        extra_bcftools_opts="",
    shell:
        """
        bcftools view {params.extra_bcftools_opts} -e 'ID=@{input.exclude}' {input.vcf} | \
            bgzip > \
            {output.vcf}
        tabix {output.vcf}
        """

rule concat_vcf:
    params:
        extra_bcftools_opts="",
    shell:
        """
        bcftools concat {params.extra_bcftools_opts} {input} | bgzip -c > {output.vcf}
        tabix {output.vcf}
        """ 

rule merge_vcf:
    params:
        extra_bcftools_opts="",
    shell:
        """
        bcftools merge {params.extra_bcftools_opts} {input.vcf} | bgzip -c > {output.vcf}
        tabix {output.vcf}
        """

rule vcf_to_plink:
    params:
        plink_exe="plink1.9",
    shell:
        """
        {params.plink_exe} --vcf {input.vcf} --make-bed --out {params.out_prefix}
        mv {params.out_prefix}.log {log}
        """

rule convert_to_gds:
    script: "scripts/convert_to_gds.R"

rule seqMissing:
    script: "scripts/seqMissing.R"

rule seqAlleleFreq:
    script: "scripts/seqAlleleFreq.R"

rule heterozygosity:
    script: "scripts/heterozygosity.R"

rule hwe:
    params:
        seed=51,
    script: "scripts/hwe.R"

rule get_var_data:
    params:
        build="hg38",
    script: "scripts/get_var_data.R"

rule combine_varmet:
    script: "scripts/combine_varmet.R"

rule parse_sexcheck:
    script: "scripts/parse_sexcheck.R"

rule snpgdsLDpruning:
    params:
        seed=51,
    script: "scripts/ld_prune.R"

rule snpgdsIBDKING:
    script: "scripts/king.R"

rule king_detect_dups_mz:
    params:
        threshold=(0.5 * 0.25) ** 0.5 # Geometric mean of dup/MZ and 1st degree thresholds
    script: "scripts/king_detect_dups_mz.R"

rule pcair:
    script: "scripts/pcair.R"

rule pcrelate:
    params:
        verbose=True,
    script: "scripts/pcrelate.R"
